{% if row.packeta %}
	Packeta:
	{{ row.packeta['place'] is defined ? row.packeta['place'] }},
	{% if row.packeta['nameStreet'] is defined %}
		{{ row.packeta['nameStreet'] }}
	{% elseif row.packeta['name'] is defined %}
		{{ row.packeta['name'] }}
	{% endif %}
{% endif %}
